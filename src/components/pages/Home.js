import React, { Fragment } from "react";

import FakeText from "./FakeText";

const Home = () => (
  <Fragment>
    <h1>Home page - React Router Hooks</h1>
    <hr />
    <FakeText />
  </Fragment>
);

export default Home;
